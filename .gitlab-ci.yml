image: node:18

stages:
  - deploy

pages:
  stage: deploy

  script:
    - export YOUTUBE_API_KEY=$YOUTUBE_API_KEY
    - export YOUTUBE_CHANNEL_ID=$YOUTUBE_CHANNEL_ID
    - npm ci
    - npm run build
    - cp -r dist/* public/
    - rm -rf dist

  artifacts:
    paths:
      - public

  only:
    - main
