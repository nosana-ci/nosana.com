image: node:18

stages:
  - deploy

pages:
  stage: deploy

  script:
    - export YOUTUBE_API_KEY=$YOUTUBE_API_KEY
    - export YOUTUBE_CHANNEL_ID=$YOUTUBE_CHANNEL_ID
    - npm ci
    - npm run build
    - mv dist public

  artifacts:
    paths:
      - public

  only:
    - main